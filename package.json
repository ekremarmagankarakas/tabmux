{
  "name": "tabmux",
  "private": true,
  "version": "0.1.0",
  "description": "Drive Chrome from the keyboard like tmux: a prefix key, modal commands, tab groups, and saved sessions.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
